import { component$ } from '@builder.io/qwik';

export const Footer = component$(() => {
  return (
    <footer class='w-full h-48 bg-black text-white flex justify-center items-center'>
      <span>This is a first proyect with qwik</span>
    </footer>
  );
});
